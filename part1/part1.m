# Amer Sulieman# Sep. 08 2019#PART 1: Find the Mean Squared Error       #When coefficients of linear model are computed      #Using exact solution B = inv(x' * x) * x' *y      #load the data of each filex = load("-ascii", "../HousingData/X.txt");y= load("-ascii", "../HousingData/Y.txt");#Assign beta to be the exact solution formulaB = inv(x' * x) * x' *y;#getting number of rows in x for MSE[N,M] = size(x);#RSS = (Y - Y')^2 but since we can't do square, we use transpose multiplicationErr = (y-x*B)' * (y - x*B);#MSE = ERR/N , where N represent number of rowsMSE = Err/N;#Now we can take the root of our MSE since we squared it to avoid errosroot_MSE = sqrt(MSE);#Write data to a filefile_dp = fopen('Output_MSE.txt', 'w');file_data = "Betas:\n\t%d, %d, %d, %d\nRSS = %d\nMSE = %d\nROOT_MSE = %d";fprintf(file_dp,file_data,B, Err, MSE, root_MSE);fclose(file_dp);